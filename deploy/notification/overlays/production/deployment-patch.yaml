apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: notification
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 512Mi
