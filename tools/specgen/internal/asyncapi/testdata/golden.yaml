# DO NOT EDIT — generated by tools/specgen.
# Source: services/fixture/internal/adapter/outbound/messaging/exposed.go
asyncapi: 3.1.0
info:
  title: fixture
  version: 0.1.0
  description: fixture — generated by tools/specgen
  contact:
    name: bookinfo-team
    url: https://github.com/kaio6fellipe/event-driven-bookinfo
    email: noreply@bookinfo.local
  license:
    name: Apache-2.0
    url: https://www.apache.org/licenses/LICENSE-2.0
  tags:
    - name: things
servers:
  kafka:
    host: bookinfo-kafka-kafka-bootstrap.platform.svc.cluster.local:9092
    protocol: kafka
    description: Local Kafka bootstrap
channels:
  events:
    address: fixture_things_events
    messages:
      thing-created:
        $ref: '#/components/messages/thing-created'
operations:
  send_events:
    action: send
    description: Publish events from the fixture service
    tags:
      - name: things
    channel:
      $ref: '#/channels/events'
components:
  messages:
    thing-created:
      name: thing-created
      title: thing-created
      summary: Emitted when a thing is created.
      description: Emitted when a thing is created.
      contentType: application/json
      payload:
        $ref: '#/components/schemas/ThingCreatedPayload'
      headers:
        type: object
        properties:
          ce-source:
            type: string
            const: fixture
          ce-specversion:
            type: string
            const: "1.0"
          ce-type:
            type: string
            const: com.fixture.thing-created
      tags:
        - name: things
  schemas:
    ThingCreatedPayload:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
      required:
        - id
        - name
