apiVersion: apps/v1
kind: Deployment
metadata:
  name: reviews
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: reviews
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits: {}
