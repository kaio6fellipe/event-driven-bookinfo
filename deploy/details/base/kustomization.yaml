apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - eventsource.yaml
  - sensor.yaml

commonLabels:
  app: details
  part-of: event-driven-bookinfo
