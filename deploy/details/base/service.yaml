apiVersion: v1
kind: Service
metadata:
  name: details
  labels:
    app: details
    part-of: event-driven-bookinfo
spec:
  type: ClusterIP
  selector:
    app: details
  ports:
    - name: http
      port: 80
      targetPort: 8080
