apiVersion: v1
kind: Service
metadata:
  name: reviews
  labels:
    app: reviews
    part-of: event-driven-bookinfo
spec:
  type: ClusterIP
  selector:
    app: reviews
  ports:
    - name: http
      port: 80
      targetPort: 8080
