apiVersion: v1
kind: Service
metadata:
  name: dlqueue
  labels:
    app: dlqueue
    part-of: event-driven-bookinfo
spec:
  type: ClusterIP
  selector:
    app: dlqueue
  ports:
    - name: http
      port: 80
      targetPort: 8080
