# DO NOT EDIT — generated by tools/specgen.
# Source: services/reviews/internal/adapter/outbound/kafka/exposed.go
asyncapi: 3.0.0
info:
  title: reviews
  version: 1.0.0
channels:
  events:
    address: bookinfo_reviews_events
    messages:
      review-deleted:
        $ref: '#/components/messages/review-deleted'
      review-submitted:
        $ref: '#/components/messages/review-submitted'
operations:
  send_events:
    action: send
    channel:
      $ref: '#/channels/events'
components:
  messages:
    review-deleted:
      name: review-deleted
      title: review-deleted
      summary: Emitted after a successful DeleteReview call.
      contentType: application/json
      payload:
        $ref: '#/components/schemas/ReviewDeletedPayload'
      headers:
        type: object
        properties:
          ce-source:
            type: string
            const: reviews
          ce-specversion:
            type: string
            const: "1.0"
          ce-type:
            type: string
            const: com.bookinfo.reviews.review-deleted
    review-submitted:
      name: review-submitted
      title: review-submitted
      summary: Emitted after a successful SubmitReview call.
      contentType: application/json
      payload:
        $ref: '#/components/schemas/ReviewSubmittedPayload'
      headers:
        type: object
        properties:
          ce-source:
            type: string
            const: reviews
          ce-specversion:
            type: string
            const: "1.0"
          ce-type:
            type: string
            const: com.bookinfo.reviews.review-submitted
  schemas:
    ReviewDeletedPayload:
      type: object
      properties:
        idempotency_key:
          type: string
        product_id:
          type: string
        review_id:
          type: string
      required:
        - review_id
        - idempotency_key
    ReviewSubmittedPayload:
      type: object
      properties:
        id:
          type: string
        idempotency_key:
          type: string
        product_id:
          type: string
        reviewer:
          type: string
        text:
          type: string
      required:
        - product_id
        - reviewer
        - text
        - idempotency_key
