# DO NOT EDIT — generated by tools/specgen.
# Source: services/ingestion/internal/adapter/outbound/messaging/exposed.go
asyncapi: 3.1.0
info:
  title: ingestion
  version: 1.0.0
  description: ingestion — generated by tools/specgen
  contact:
    name: bookinfo-team
    url: https://github.com/kaio6fellipe/event-driven-bookinfo
    email: noreply@bookinfo.local
  license:
    name: Apache-2.0
    url: https://www.apache.org/licenses/LICENSE-2.0
  tags:
    - name: ingestion
servers:
  jetstream:
    host: nats://nats.platform.svc.cluster.local:4222
    protocol: nats
    description: Local NATS JetStream (jetstream cluster)
  kafka:
    host: bookinfo-kafka-kafka-bootstrap.platform.svc.cluster.local:9092
    protocol: kafka
    description: Local Kafka bootstrap (kafka cluster)
channels:
  raw-books-details:
    address: raw_books_details
    messages:
      book-added:
        $ref: '#/components/messages/book-added'
operations:
  send_raw-books-details:
    action: send
    description: Publish events from the ingestion service
    tags:
      - name: ingestion
    channel:
      $ref: '#/channels/raw-books-details'
components:
  messages:
    book-added:
      name: book-added
      title: book-added
      summary: Emitted for every Book scraped from Open Library.
      description: Emitted for every Book scraped from Open Library.
      contentType: application/json
      payload:
        $ref: '#/components/schemas/BookEvent'
      headers:
        type: object
        properties:
          ce-source:
            type: string
            const: ingestion
          ce-specversion:
            type: string
            const: "1.0"
          ce-type:
            type: string
            const: com.bookinfo.ingestion.book-added
      tags:
        - name: ingestion
  schemas:
    BookEvent:
      type: object
      properties:
        author:
          type: string
        idempotency_key:
          type: string
        isbn_10:
          type: string
        isbn_13:
          type: string
        language:
          type: string
        pages:
          type: integer
          format: int32
        publisher:
          type: string
        title:
          type: string
        type:
          type: string
        year:
          type: integer
          format: int32
      required:
        - title
        - author
        - year
        - type
        - idempotency_key
