apiVersion: v1
kind: Service
metadata:
  name: reviews-write
  namespace: bookinfo
  labels:
    app: reviews
    role: write
    part-of: event-driven-bookinfo
spec:
  type: ClusterIP
  selector:
    app: reviews
    role: write
  ports:
    - name: http
      port: 80
      targetPort: 8080
