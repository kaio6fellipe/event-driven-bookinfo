# DO NOT EDIT — generated by tools/specgen.
# Source: services/ingestion/internal/adapter/inbound/http/endpoints.go
openapi: 3.1.0
info:
  title: ingestion
  version: 1.0.0
  description: ingestion — generated by tools/specgen
  contact:
    name: bookinfo-team
    url: https://github.com/kaio6fellipe/event-driven-bookinfo
    email: noreply@bookinfo.local
  license:
    name: Apache-2.0
    url: https://www.apache.org/licenses/LICENSE-2.0
servers:
  - description: Local k3d gateway
    url: http://localhost:8080
tags:
  - name: ingestion
paths:
  /v1/ingestion/status:
    get:
      description: Get current scraper state and last result
      operationId: getV1IngestionStatus
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatusResponse'
          description: success
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Internal Server Error
      summary: Get current scraper state and last result
      tags:
        - ingestion
  /v1/ingestion/trigger:
    post:
      description: Trigger a one-shot scrape (optionally overriding queries)
      operationId: postV1IngestionTrigger
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TriggerRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScrapeResultResponse'
          description: success
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Bad Request
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Conflict
      summary: Trigger a one-shot scrape (optionally overriding queries)
      tags:
        - ingestion
components:
  schemas:
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
    ScrapeResultResponse:
      type: object
      properties:
        books_found:
          type: integer
          format: int32
        duration_ms:
          type: integer
          format: int64
        errors:
          type: integer
          format: int32
        events_published:
          type: integer
          format: int32
      required:
        - books_found
        - events_published
        - errors
        - duration_ms
    StatusResponse:
      type: object
      properties:
        last_result:
          type: object
          properties:
            books_found:
              type: integer
              format: int32
            duration_ms:
              type: integer
              format: int64
            errors:
              type: integer
              format: int32
            events_published:
              type: integer
              format: int32
          required:
            - books_found
            - events_published
            - errors
            - duration_ms
        last_run_at:
          type: string
          format: date-time
        state:
          type: string
      required:
        - state
    TriggerRequest:
      type: object
      properties:
        queries:
          type: array
          items:
            type: string
