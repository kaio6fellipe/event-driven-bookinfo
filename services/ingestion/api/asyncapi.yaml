# DO NOT EDIT — generated by tools/specgen.
# Source: services/ingestion/internal/adapter/outbound/kafka/exposed.go
asyncapi: 3.0.0
info:
  title: ingestion
  version: 1.0.0
channels:
  raw-books-details:
    address: raw_books_details
    messages:
      book-added:
        $ref: '#/components/messages/book-added'
operations:
  send_raw-books-details:
    action: send
    channel:
      $ref: '#/channels/raw-books-details'
components:
  messages:
    book-added:
      name: book-added
      title: book-added
      summary: Emitted for every Book scraped from Open Library.
      contentType: application/json
      payload:
        $ref: '#/components/schemas/BookEvent'
      headers:
        type: object
        properties:
          ce-source:
            type: string
            const: ingestion
          ce-specversion:
            type: string
            const: "1.0"
          ce-type:
            type: string
            const: com.bookinfo.ingestion.book-added
  schemas:
    BookEvent:
      type: object
      properties:
        author:
          type: string
        idempotency_key:
          type: string
        isbn_10:
          type: string
        isbn_13:
          type: string
        language:
          type: string
        pages:
          type: integer
          format: int32
        publisher:
          type: string
        title:
          type: string
        type:
          type: string
        year:
          type: integer
          format: int32
      required:
        - title
        - author
        - year
        - type
        - idempotency_key
