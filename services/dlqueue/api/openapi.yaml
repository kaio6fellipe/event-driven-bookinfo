# DO NOT EDIT — generated by tools/specgen.
# Source: services/dlqueue/internal/adapter/inbound/http/endpoints.go
openapi: 3.1.0
info:
  title: dlqueue
  version: 1.0.0
  description: dlqueue — generated by tools/specgen
  contact:
    name: bookinfo-team
    url: https://github.com/kaio6fellipe/event-driven-bookinfo
    email: noreply@bookinfo.local
  license:
    name: Apache-2.0
    url: https://www.apache.org/licenses/LICENSE-2.0
servers:
  - description: Local k3d gateway
    url: http://localhost:8080
tags:
  - name: dlqueue
paths:
  /v1/events:
    get:
      description: List DLQ events with filters and pagination
      operationId: getV1Events
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListEventsResponse'
          description: success
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Internal Server Error
      summary: List DLQ events with filters and pagination
      tags:
        - dlqueue
    post:
      description: Ingest a failed event from a Sensor's dlqTrigger
      operationId: postV1Events
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IngestEventRequest'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DLQEventResponse'
          description: success
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Bad Request
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Internal Server Error
      summary: Ingest a failed event from a Sensor's dlqTrigger
      tags:
        - dlqueue
      x-bookinfo-event-name: dlq-event-received
  /v1/events/batch/replay:
    post:
      description: Replay all events matching a filter
      operationId: postV1EventsBatchReplay
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BatchReplayRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BatchActionResponse'
          description: success
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Bad Request
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Internal Server Error
      summary: Replay all events matching a filter
      tags:
        - dlqueue
  /v1/events/batch/resolve:
    post:
      description: Resolve a batch of events by IDs
      operationId: postV1EventsBatchResolve
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BatchResolveRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BatchActionResponse'
          description: success
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Bad Request
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Internal Server Error
      summary: Resolve a batch of events by IDs
      tags:
        - dlqueue
  /v1/events/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
    get:
      description: Get a single DLQ event by ID
      operationId: getV1EventsId
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DLQEventResponse'
          description: success
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Not Found
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Internal Server Error
      summary: Get a single DLQ event by ID
      tags:
        - dlqueue
  /v1/events/{id}/replay:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
    post:
      description: Replay a failed event back to its original target
      operationId: postV1EventsIdReplay
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DLQEventResponse'
          description: success
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Not Found
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Conflict
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Internal Server Error
      summary: Replay a failed event back to its original target
      tags:
        - dlqueue
  /v1/events/{id}/reset:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
    post:
      description: Reset a poisoned event back to pending
      operationId: postV1EventsIdReset
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DLQEventResponse'
          description: success
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Not Found
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Conflict
      summary: Reset a poisoned event back to pending
      tags:
        - dlqueue
  /v1/events/{id}/resolve:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
    post:
      description: Mark an event as resolved (terminal state)
      operationId: postV1EventsIdResolve
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResolveRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DLQEventResponse'
          description: success
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Bad Request
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Not Found
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Conflict
      summary: Mark an event as resolved (terminal state)
      tags:
        - dlqueue
components:
  schemas:
    BatchActionResponse:
      type: object
      properties:
        affected_count:
          type: integer
          format: int32
      required:
        - affected_count
    BatchReplayRequest:
      type: object
      properties:
        event_source:
          type: string
        failed_trigger:
          type: string
        limit:
          type: integer
          format: int32
        sensor_name:
          type: string
        status:
          type: string
    BatchResolveRequest:
      type: object
      properties:
        ids:
          type: array
          items:
            type: string
        resolved_by:
          type: string
      required:
        - ids
        - resolved_by
    DLQEventResponse:
      type: object
      properties:
        created_at:
          type: string
          format: date-time
        datacontenttype:
          type: string
        event_id:
          type: string
        event_source:
          type: string
        event_subject:
          type: string
        event_timestamp:
          type: string
          format: date-time
        event_type:
          type: string
        eventsource_url:
          type: string
        failed_trigger:
          type: string
        id:
          type: string
        last_replayed_at:
          type: string
          format: date-time
        max_retries:
          type: integer
          format: int32
        namespace:
          type: string
        notes:
          type: string
        original_headers:
          type: object
        original_payload:
          type: object
        payload_hash:
          type: string
        resolved_at:
          type: string
          format: date-time
        resolved_by:
          type: string
        retry_count:
          type: integer
          format: int32
        sensor_name:
          type: string
        status:
          type: string
        updated_at:
          type: string
          format: date-time
      required:
        - id
        - event_id
        - event_type
        - event_source
        - event_subject
        - sensor_name
        - failed_trigger
        - eventsource_url
        - namespace
        - original_payload
        - payload_hash
        - original_headers
        - datacontenttype
        - event_timestamp
        - status
        - retry_count
        - max_retries
        - created_at
        - updated_at
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
    IngestEventRequest:
      type: object
      properties:
        datacontenttype:
          type: string
        event_id:
          type: string
        event_source:
          type: string
        event_subject:
          type: string
        event_timestamp:
          type: string
        event_type:
          type: string
        eventsource_url:
          type: string
        failed_trigger:
          type: string
        namespace:
          type: string
        original_headers:
          type: object
        original_payload:
          type: object
        sensor_name:
          type: string
      required:
        - event_id
        - event_type
        - event_source
        - event_subject
        - sensor_name
        - failed_trigger
        - eventsource_url
        - namespace
        - original_payload
        - original_headers
        - datacontenttype
        - event_timestamp
    ListEventsResponse:
      type: object
      properties:
        items:
          type: array
          items:
            type: object
            properties:
              created_at:
                type: string
                format: date-time
              datacontenttype:
                type: string
              event_id:
                type: string
              event_source:
                type: string
              event_subject:
                type: string
              event_timestamp:
                type: string
                format: date-time
              event_type:
                type: string
              eventsource_url:
                type: string
              failed_trigger:
                type: string
              id:
                type: string
              last_replayed_at:
                type: string
                format: date-time
              max_retries:
                type: integer
                format: int32
              namespace:
                type: string
              notes:
                type: string
              original_headers:
                type: object
              original_payload:
                type: object
              payload_hash:
                type: string
              resolved_at:
                type: string
                format: date-time
              resolved_by:
                type: string
              retry_count:
                type: integer
                format: int32
              sensor_name:
                type: string
              status:
                type: string
              updated_at:
                type: string
                format: date-time
            required:
              - id
              - event_id
              - event_type
              - event_source
              - event_subject
              - sensor_name
              - failed_trigger
              - eventsource_url
              - namespace
              - original_payload
              - payload_hash
              - original_headers
              - datacontenttype
              - event_timestamp
              - status
              - retry_count
              - max_retries
              - created_at
              - updated_at
        limit:
          type: integer
          format: int32
        offset:
          type: integer
          format: int32
        total_items:
          type: integer
          format: int32
      required:
        - items
        - total_items
        - offset
        - limit
    ResolveRequest:
      type: object
      properties:
        resolved_by:
          type: string
      required:
        - resolved_by
