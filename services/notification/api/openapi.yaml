# DO NOT EDIT — generated by tools/specgen.
# Source: services/notification/internal/adapter/inbound/http/endpoints.go
openapi: 3.1.0
info:
  title: notification
  version: 1.0.0
  description: notification — generated by tools/specgen
  contact:
    name: bookinfo-team
    url: https://github.com/kaio6fellipe/event-driven-bookinfo
    email: noreply@bookinfo.local
  license:
    name: Apache-2.0
    url: https://www.apache.org/licenses/LICENSE-2.0
servers:
  - description: Local k3d gateway
    url: http://localhost:8080
tags:
  - name: notification
paths:
  /v1/notifications:
    get:
      description: List notifications for a recipient (?recipient=<email>)
      operationId: getV1Notifications
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationsListResponse'
          description: success
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Bad Request
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Internal Server Error
      summary: List notifications for a recipient (?recipient=<email>)
      tags:
        - notification
    post:
      description: Dispatch a notification (called by Argo Events sensors)
      operationId: postV1Notifications
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DispatchNotificationRequest'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationResponse'
          description: success
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Bad Request
      summary: Dispatch a notification (called by Argo Events sensors)
      tags:
        - notification
  /v1/notifications/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
    get:
      description: Get a single notification by ID
      operationId: getV1NotificationsId
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationResponse'
          description: success
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: Not Found
      summary: Get a single notification by ID
      tags:
        - notification
components:
  schemas:
    DispatchNotificationRequest:
      type: object
      properties:
        body:
          type: string
        channel:
          type: string
        idempotency_key:
          type: string
        recipient:
          type: string
        subject:
          type: string
      required:
        - recipient
        - channel
        - subject
        - body
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
    NotificationResponse:
      type: object
      properties:
        body:
          type: string
        channel:
          type: string
        id:
          type: string
        recipient:
          type: string
        sent_at:
          type: string
          format: date-time
        status:
          type: string
        subject:
          type: string
      required:
        - id
        - recipient
        - channel
        - subject
        - body
        - status
    NotificationsListResponse:
      type: object
      properties:
        notifications:
          type: array
          items:
            type: object
            properties:
              body:
                type: string
              channel:
                type: string
              id:
                type: string
              recipient:
                type: string
              sent_at:
                type: string
                format: date-time
              status:
                type: string
              subject:
                type: string
            required:
              - id
              - recipient
              - channel
              - subject
              - body
              - status
      required:
        - notifications
