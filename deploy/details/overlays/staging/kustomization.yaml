apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: details

configMapGenerator:
  - name: details
    behavior: merge
    literals:
      - LOG_LEVEL=info
