apiVersion: v1
kind: Service
metadata:
  name: notification
  labels:
    app: notification
    part-of: event-driven-bookinfo
spec:
  type: ClusterIP
  selector:
    app: notification
  ports:
    - name: http
      port: 80
      targetPort: 8080
