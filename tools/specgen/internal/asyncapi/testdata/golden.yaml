# DO NOT EDIT — generated by tools/specgen.
# Source: services/fixture/internal/adapter/outbound/kafka/exposed.go
asyncapi: 3.0.0
info:
  title: fixture
  version: 0.1.0
channels:
  events:
    address: fixture_things_events
    messages:
      thing-created:
        $ref: '#/components/messages/thing-created'
operations:
  send_events:
    action: send
    channel:
      $ref: '#/channels/events'
components:
  messages:
    thing-created:
      name: thing-created
      title: thing-created
      summary: Emitted when a thing is created.
      contentType: application/json
      payload:
        $ref: '#/components/schemas/ThingCreatedPayload'
      headers:
        type: object
        properties:
          ce-source:
            type: string
            const: fixture
          ce-specversion:
            type: string
            const: "1.0"
          ce-type:
            type: string
            const: com.fixture.thing-created
  schemas:
    ThingCreatedPayload:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
      required:
        - id
        - name
