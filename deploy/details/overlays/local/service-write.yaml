apiVersion: v1
kind: Service
metadata:
  name: details-write
  namespace: bookinfo
  labels:
    app: details
    role: write
    part-of: event-driven-bookinfo
spec:
  type: ClusterIP
  selector:
    app: details
    role: write
  ports:
    - name: http
      port: 80
      targetPort: 8080
