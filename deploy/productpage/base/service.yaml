apiVersion: v1
kind: Service
metadata:
  name: productpage
  labels:
    app: productpage
    part-of: event-driven-bookinfo
spec:
  type: ClusterIP
  selector:
    app: productpage
  ports:
    - name: http
      port: 80
      targetPort: 8080
