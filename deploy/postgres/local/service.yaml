apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: bookinfo
  labels:
    app: postgres
    part-of: event-driven-bookinfo
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
