apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: reviews

configMapGenerator:
  - name: reviews
    behavior: merge
    literals:
      - LOG_LEVEL=debug
