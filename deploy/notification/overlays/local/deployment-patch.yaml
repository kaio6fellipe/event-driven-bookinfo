apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification
  labels:
    role: write
spec:
  replicas: 1
  template:
    metadata:
      labels:
        role: write
    spec:
      containers:
        - name: notification
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits: {}
